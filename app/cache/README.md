# Cache obsolète (pré-v1.0)

Le cache runtime est **`var/cache/`** (`APP_CACHE_DIR`).

Les fichiers JSON ici sont d’anciennes copies locales ; ils ne sont plus lus
par l’application. Tu peux les supprimer sans risque.
